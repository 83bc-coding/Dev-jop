 import FilterIcon from "../../icons/FilterIcon";
import Button from "../../ui/Button";

const ButtonFilter = () => {
  return (
    <Button>
      <FilterIcon />
    </Button>
  );
};

export default ButtonFilter;
