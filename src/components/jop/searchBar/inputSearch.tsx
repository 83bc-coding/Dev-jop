import React from 'react'
import Input from '../../ui/Input'
import SearchIcon from '../../ui/SearchIcon'

const InputSearch = () => {
  return (
 <Input title='Filter by title ..'>
 <SearchIcon color='blue'/>
 </Input>
  )
}

export default InputSearch